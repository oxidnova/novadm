package internal

// Version is set by the build scripts.
var Version = "dev"

// GOVersion is set by the build scripts.
var GOVersion = "local"

// Commit is set by the build scripts.
var Commit = "dev"
